# /etc/mcp_agent_mail/env - mode 0600, values from Infisical HERMES prod
# Bearer token every city agent presents to the fabric (shared/agent-mail-client.js
# reads it as AGENT_MAIL_TOKEN).
HTTP_BEARER_TOKEN=<infisical:HERMES/prod/AGENT_MAIL_BEARER>
