# Free routing

Schema checks, hashes, diffs, heartbeat rules, duplicate detection, and report rendering run locally. Optional synthesis may use an explicitly free route only. Paid/model training routes are deny-by-default. No unavailable free route may silently fall through to paid inference.
