# Finding shape

A finding states: observed fact, exact evidence refs, affected goal, deterministic severity, pattern count, separately labeled hypothesis, recommended change, expected gain, risk, and `approval_required: true`. One finding per file; later changes supersede by link rather than overwrite history.
