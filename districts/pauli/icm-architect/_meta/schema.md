# Schema

Node types: `watcher`, `fleet-agent`, `data-asset`. Every node declares source, truth status, sensitivity, consumes, and produces. Finding frontmatter fields: id, status, severity, pattern_count, evidence_refs, affected_goal_refs, hypothesis, recommended_change, expected_gain, risk, approval_required, created_at, content_hash.
