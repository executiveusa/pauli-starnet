# Faceless channel pipeline

Flow: charter → outlier research → concept → package → research → script → visual/voice plan → production → rights/policy QA → publish gate → measure.

Faceless is a format, not a shortcut. The output must have original authorship, a factual point of view, meaningful visual variation and an explicit rights trail.
