# Client channel pipeline
Flow: brief → research → strategy → package → script → produce → QA → approval → measure.
No external/client send and no publication is part of a stage unless its gate is explicitly approved.
