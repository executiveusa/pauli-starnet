# Brand and operator training pipeline

Flow: orient → learn the system → shadow a run → produce a sandbox package → certify.
Certification is artifact-based. Watching videos is input, not proof of competence.
